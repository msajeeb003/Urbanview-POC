# frontend/ — public map (reserved)

Build plan track "Frontend (public map)", 55 h: Next.js + TypeScript + Tailwind + shadcn/ui,
Mapbox GL JS + PMTiles, PDF.js for the source-document viewer.

Consumes the backend API (`GET /v1/locate`, `GET /v1/locate/parcel`, `GET /v1/municipality`).
Design reference: `docs/wireframe/wireframe-decoded.html` and the tokens recorded in `CLAUDE.md`.
Not started yet (priority P2, after the PDF → review → publish pipeline).
