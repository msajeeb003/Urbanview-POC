# admin/ — staff tool (reserved)

Build plan track "Admin", 34 h: Next.js + Auth.js magic-link login with `admin` / `reviewer` /
`expert` roles. Upload planning PDFs, run AI extraction, review 100% of extracted values against
the cited PDF page, publish approved data to the map, manage financial assumptions and orders.
Not started yet (priority P1, alongside the GIS + extraction pipeline in `backend/jobs`).
Chrome follows the wireframe's admin console exactly (`docs/specs/frontend-design.md` §5,
`docs/wireframe/screens/admin*.png`).
